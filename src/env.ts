export const HOST: string = process.env.HOST || 'localhost'
export const PORT: number = Number(process.env.PORT) || 3000

export const DISTANCE_RANGE: number = Number(process.env.DISTANCE_RANGE) || 10
